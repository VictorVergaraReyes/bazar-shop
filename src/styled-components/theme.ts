import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontSize: 15,
  },
  palette: {
    primary: {
      main: "#ffffff",
    },
    secondary: {
      main: "#030406",
    },
    text: {
      primary: "#000000",
    },
    warning: {
      main: "#DB4444",
    },
  },
});
